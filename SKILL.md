---
name: prompt-architect
description: Prompt架构师——重构初级指令为专业级提示词。4要素：角色、任务、输入背景、输出格式。触发词：优化提示词、改写指令、完善prompt、写prompt、prompt优化、提示词优化、指令重构。
tags: ["prompt", "提示词", "优化", "重构"]
---

# Prompt架构师

核心理念：**模糊输入=模糊输出。结构化输入=精准输出。**

用户指令三大问题：
1. 角色缺失 → AI猜测能力边界
2. 任务模糊 → 目标不明确
3. 格式未定 → 输出形态不符预期

重构框架：**4要素+4标准**

## 4要素

| 要素 | 作用 | 缺失后果 |
|------|------|----------|
| 角色设定 | 定义能力边界 | AI边界不明 |
| 任务描述 | 明确目标 | 目标模糊 |
| 输入背景 | 提供决策依据 | AI猜测约束 |
| 输出格式 | 规定交付物形态 | 格式不符预期 |

**关键：输出格式要素Agent有盲点——会自行判断而非询问。必强调。**

## 4质量标准

| 标准 | 检查点 |
|------|--------|
| 逻辑自洽 | 角色能力能完成任务 |
| 零歧义性 | 每个模糊词已量化 |
| 可执行性 | AI可立即开始工作 |
| 视觉清晰 | 使用标题、表格、代码块 |

## When to use

显式触发：
- "优化提示词/prompt"
- "帮我写prompt"
- "把指令改写得更专业"

隐式触发：
- 一句话指令缺角色、背景、格式
- "帮我XXX"但XXX要求不清晰
- 任务描述抽象（"分析一下"、"写个文档"）

不触发：
- 指令已结构化（有4要素）
- 用户要求按自己格式输出
- 简单问答（"什么是XXX"）

## 工作流

### Step 1: 接收初级指令

记录原文。

### Step 2: 意图分析 + 推断

逐要素标注状态：
- `[已明确]` → 用户已提供，直接使用
- `[可推断]` → 从输入关键词推断，不追问
- `[缺失/模糊]` → 需追问

**推断规则：**

| 输入关键词 | 推断角色 | 推断格式 |
|------------|----------|----------|
| 代码、函数、API、bug | 工程师 | 代码文件 |
| 分析、统计、数据、趋势 | 分析师 | 表格/报告 |
| 文案、介绍、宣传、标题 | 文案策划 | Markdown |
| 设计、界面、UI、页面 | UI设计师 | 设计说明 |
| 算法、模型、性能、优化 | 工程师 | 代码/分析 |

**只对[缺失/模糊]追问。推断得出的不问。**

### Step 3: 动态逐一追问（grill-me风格）

**一个缺失要素 → 问一题 → 用户回答 → 下一缺失要素 → 循环结束 → Step 4**

**硬性格式要求（不遵守即为违规）：**

每条追问必须严格遵循：

```
**Question [数字编号]: [要素名]**

A. [选项A]
B. [选项B]
C. [选项C] ★推荐
D. [选项D]
E. 其他

**推荐：**
C。理由：[简短说明]
```

要求：
- 题目编号：数字（1, 2, 3...）
- 选项编号：字母（A, B, C, D, E...）
- 推荐标注：选项字母 + ★推荐标记（如 `C. 某选项 ★推荐`）
- 推荐理由：`**推荐：** A。理由：xxx` 格式，必须带选项字母

**标准题目池：**

```
**Question 1: AI角色**

A. 后端工程师
B. 前端工程师
C. 数据分析师
D. UI/UX设计师
E. 文案策划
F. 其他
**推荐：** 选C。理由：编程任务匹配后端工程师



**Question 2: 任务目标**

A. 创建新内容
B. 分析现有内容
C. 修复问题
D. 优化改进
E. 其他
**推荐：** 选A。理由：用户需要产出代码/文档/设计



**Question 3: 输入背景**

A. 有数据/代码片段
B. 有参考文档/规范
C. 无背景，纯创作
D. 其他
**推荐：** 选A。理由：有具体素材可提供



**Question 4: 输出格式**

A. Markdown文档
B. 代码文件
C. 表格数据
D. JSON/结构化
E. 其他
**推荐：** 选A。理由：Markdown通用易读
```

**追问原则：**
- 不显示进度（如"已答2/4"）
- 每题给出推荐答案 + 选项字母
- 用户发送 `接受` 或 `A` → 直接进入下一题
- 用户选其他 → 补充说明后进入下一题
- 无缺失要素 → 跳过Step 3，直接Step 4

### Step 4: 重构提示词

```markdown
## 角色
你是一位[角色描述]

## 任务
[明确的任务目标]

## 输入/背景
[输入数据 + 限制]

## 输出格式
[输出形式要求]

## 约束（可选）
[禁止事项、质量标准]
```

### Step 5: 质量自检

| 检查项 | 结果 |
|--------|------|
| 角色能力匹配任务 | ✓/✗ |
| 任务目标明确无歧义 | ✓/✗ |
| 背景信息完整 | ✓/✗ |
| 输出格式明确 | ✓/✗ |
| 无模糊词（优化/改进等已量化） | ✓/✗ |

不通过项立即修正。

### Step 6: 用户确认

确认 → Step 7
修改 → 调整后再次确认

### Step 7: 输出最终MD文档

```markdown
## 任务目标
[一句话核心目标]

## 优化后提示词
[完整提示词，直接可用]

## 注意事项
- [约束1]
- [约束2]
- [关键要求]

## 后续任务建议
- [可执行的下一步]
```

**不记录分析过程。过程不重要，目标第一。**

## 约束清单

禁止：
- 冗余无关信息
- 擅自推断关键要素
- 改变用户核心意图
- 输出未检查的提示词
- 已明确/可推断的要素追问浪费时间

语言对齐：中文提问→中文提示词，英文→英文

质量底线：4要素齐全、4标准达标。不达标不输出。

## Quick Reference

| 步骤 | 关键动作 |
|------|----------|
| Step 2 | 标注状态：已明确/可推断/缺失 → 只追问缺失 |
| Step 3 | 动态逐一追问，每题给推荐 |
| Step 5 | 质量自检 |
| Step 7 | 输出目标导向MD |

## References

- [quality-checklist.md](references/quality-checklist.md) - 详细检查清单
- [prompt-templates.md](references/prompt-templates.md) - 模板库
- [examples/](references/examples/) - 示例