---
name: prompt-architect
description: "Prompt 架构师——将原始模糊的初级指令重构为结构化专业级的高级提示词。必须包含4要素：角色设定、任务描述、输入数据/背景、输出格式。质量标准：逻辑自洽、零歧义性、可执行性、视觉清晰。触发词：优化提示词、改写指令、完善prompt、写一个prompt、帮我写一个AI指令、prompt优化、提示词优化、指令重构、让提示词更专业、高级提示词、专业prompt、把指令改清楚。"
tags: ["prompt", "提示词", "AI指令", "优化", "重构", "结构化"]
---

# Prompt 架构师

## 核心理念

**模糊的输入 = 模糊的输出。结构化输入 = 精准输出。**

大多数用户给AI的指令存在三大问题：
1. **角色缺失**：没有告诉AI"你是谁"，AI只能猜测能力边界
2. **任务模糊**：说"帮我写个东西"，不说写什么、给谁看、什么风格
3. **格式未定**：AI默认输出散文段落，但用户可能需要JSON/表格/代码

本技能将初级指令重构为专业级提示词，遵循 **"4要素+4标准"** 框架。

## 4要素（必须包含）

| 要素 | 作用 | 缺失后果 | 示例 |
|------|------|----------|------|
| 角色设定 | 定义能力边界和专业视角 | AI能力边界不明 | "你是一位10年经验的Java后端架构师" |
| 任务描述 | 明确目标，消除歧义 | 目标模糊 | "重构这段代码，使其符合Spring Boot最佳实践" |
| 输入数据/背景 | 提供决策依据 | AI只能猜测约束 | "这是一个处理订单的Service类，当前有3个问题..." |
| 输出格式 | 规定交付物形态 | 可能格式不符预期 | "输出为重构后的完整代码 + 修改说明表格" |

**关键发现（RED阶段测试）**：即使Agent在模糊指令方面主动追问，在"输出格式"要素上仍存在盲点——会自行判断而非询问用户。此要素需要特别强调。

## 4质量标准

| 标准 | 定义 | 检查点 |
|------|------|--------|
| 逻辑自洽 | 各要素不矛盾 | 角色能力能完成任务 |
| 零歧义性 | 无多种解读可能 | 每个"优化"都有明确目标 |
| 可执行性 | AI能立即开始工作 | 不需要追问更多信息 |
| 视觉清晰 | 格式整洁易读 | 使用标题、表格、代码块分隔 |

## When to use

用户以下请求时触发此技能：

**显式触发**：
- "优化这个提示词/prompt"
- "帮我写一个prompt"
- "把这个指令改写得更专业"
- "完善我的AI指令"
- "让提示词更结构化"
- "把指令改清楚"

**隐式触发**（用户给出初级指令，主动介入）：
- 用户提供一句话指令，缺少角色、背景、格式
- 用户说"帮我XXX"但没有说清楚XXX的具体要求
- 用户给AI的任务描述过于抽象（"分析一下"、"写个文档"）

**不触发**：
- 用户指令已足够结构化（有角色、任务、背景、格式）
- 用户明确要求按自己的格式输出
- 简单问答型请求（"什么是XXX"、"帮我翻译这句话"）

## 完整工作流

### Step 1: 接收初级指令

用户提供原始指令（可能是一句话、一段文字或一个模糊的请求）。

**记录原始指令**：保留原文用于后续对比验证。

### Step 2: 意图分析（核心步骤）

逐项检查原始指令是否包含4要素：

```
原始指令分析：
- 角色设定：[有/无] → [缺失影响]
- 任务描述：[清晰/模糊] → [歧义点]
- 输入背景：[有/无] → [缺失什么信息]
- 输出格式：[有/无] → [用户可能期望什么格式] ← 必须检查此项！
```

**重点检查输出格式**：即使Agent会主动追问模糊指令，在输出格式上仍有盲点。如果用户未指定格式，必须明确询问。

### Step 3: 用户确认缺失信息（必须执行）

**如果有要素缺失或模糊，必须询问用户**：

| 缺失要素 | 询问模板 |
|----------|----------|
| 角色 | "AI应该扮演什么角色？（如：资深架构师、产品经理、文案专家）" |
| 任务 | "具体目标是什么？需要达到什么效果？" |
| 背景 | "有什么输入数据、限制条件或参考资料？" |
| 格式 | "期望输出什么格式？（如：代码、表格、文档、JSON、清单）" ← 必问！ |

**分支条件**：
- 用户补充完整 → 进入Step 4
- 用户说"你自己定" → 使用默认推断（见references/prompt-templates.md）
- 用户拒绝优化 → 保留原始指令，告知风险

### Step 4: 重构提示词

根据收集的信息，按以下结构输出高级提示词：

```markdown
## 角色
你是一位[角色描述，含经验/专业领域/核心能力]

## 任务
[明确的任务目标，含具体要做什么、达到什么效果]

## 输入/背景
[输入数据描述 + 限制条件 + 参考资料]

## 输出格式
[具体的输出形式要求，含结构/风格/长度限制]

## 约束（可选）
[禁止事项、风格要求、质量标准]
```

### Step 5: 质量自检（必须执行）

对照质量清单检查重构后的提示词：

**逻辑自洽检查**：
- 角色能力是否能完成任务？
- 任务目标是否与背景信息一致？

**零歧义性检查**：
- 是否有"优化"、"改进"等模糊词？如果有，是否已量化？
- 是否有多种可能的解读？

**可执行性检查**：
- AI拿到这个提示词能否立即开始工作？
- 是否有缺失的必要信息？

**视觉清晰检查**：
- 是否使用了标题、表格、代码块？
- 结构是否一目了然？

**输出检查结果**：列出通过/未通过的项，如有问题立即修正。

### Step 6: 用户确认

输出重构后的提示词，等待用户确认：
- 用户确认 → 结束，输出最终版
- 用户提出修改 → 根据反馈调整，再次确认
- 用户要求对比 → 展示原始vs优化版本对比表

### Step 7: 输出最终版本

```markdown
## 原始指令
[原文]

## 优化后提示词
[重构后的完整提示词]

## 优化要点
- [要点1：解决了什么问题]
- [要点2：增加了什么要素]
- [要点3：消除了什么歧义]
```

## 约束清单

### 禁止事项

- **禁止冗余**：不要添加用户未提及的无关信息
- **禁止过度假设**：缺失信息必须询问用户，不得擅自推断关键要素
- **禁止改变核心意图**：优化过程中不得偏离用户的原始目标
- **禁止输出未检查的提示词**：必须执行Step 5质量自检

### 语言对齐

- 用户用中文提问 → 输出中文提示词
- 用户用英文提问 → 输出英文提示词
- 用户混合语言 → 保持用户主导语言

### 保留核心意图

**验证方法**：将优化后的提示词核心目标与原始指令对比，确认一致性。

如果优化后的任务描述与原始意图不符，必须重新分析。

### 质量底线

重构后的提示词必须满足4要素齐全、4标准达标。不达标不输出。

## Quick Reference

| 步骤 | 关键动作 | 必须执行 |
|------|----------|----------|
| Step 2 | 检查4要素 | 必查输出格式 |
| Step 3 | 询问缺失信息 | 格式必问 |
| Step 5 | 质量自检 | 必须执行 |
| Step 6 | 用户确认 | 必须等待 |

## 常见错误

| 错误 | 原因 | 修正 |
|------|------|------|
| 输出格式自行判断 | Agent盲点 | 必须询问用户格式偏好 |
| 模糊词未量化 | "优化"有多种解读 | 替换为具体目标 |
| 角色能力不匹配 | 逻辑不自洽 | 检查角色是否能完成任务 |

## References

- [quality-checklist.md](references/quality-checklist.md) - 详细质量检查清单
- [prompt-templates.md](references/prompt-templates.md) - 高级提示词模板库
- [examples/](references/examples/) - 输入/输出示例（编程/分析/创意任务）